---
phase: 01-core-logger
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - internal/handler/qso.go
  - frontend/src/lib/components/LogTable.svelte
  - frontend/src/lib/api.js
autonomous: true
requirements: [QSO-03]

must_haves:
  truths:
    - "Operator can search QSOs by callsign from a search input above the log table per QSO-03"
    - "Clicking a QSO row switches that row to inline edit mode per D-05"
    - "Saving an edit persists changes to the database via PUT /api/qso/:id"
    - "Cancel reverts to display mode without changes per D-05"
    - "Log table paginates with 50 QSOs per page and loads more on demand"
    - "No modal or separate edit page — editing happens inline in the table per D-05"
  artifacts: []
  key_links:
    - from: "LogTable row click"
      to: "inline edit mode (row transforms to editable inputs)"
      via: "editingId $state tracking which row is being edited"
    - from: "inline save button"
      to: "PUT /api/qso/:id"
      via: "updateQso API call"
    - from: "search input"
      to: "GET /api/qso?search= callsign filter"
      via: "fetchQsos with search param"
---

<objective>
Add QSO search and inline editing: server-side search by callsign and update endpoints, client-side search input, click-to-edit rows per D-05, and offset-based pagination with 50 QSOs per page.

Purpose: Operators need to find and correct typos during a 27-hour contest. Inline editing per D-05 keeps workflow fast — no modal interruptions.
Output: Searchable, paginated log table where clicking a row switches to editable inputs with Save/Cancel.
</objective>

<execution_context>
@/home/jeremy/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/jeremy/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-logger/01-CONTEXT.md (D-05 inline editing, pagination discretion)
@.planning/phases/01-core-logger/01-RESEARCH.md (QSO schema, chi URL params)
@/home/jeremy/field-day-logger-plan.md

<interfaces>
From Plan 01:

internal/handler/qso.go has CreateQSO and ListQSOs. This plan extends ListQSOs with ?search= param and adds UpdateQSO handler.

internal/model/qso.go QSO struct:
```go
type QSO struct {
    ID, Timestamp, Callsign, Band, Mode, SentExchange, RecvExchange, Operator string/int/bool
}
```

frontend/src/lib/components/LogTable.svelte — current read-only table rendering qsos array.

frontend/src/lib/api.js — currently has createQSO, fetchQsos (basic). Plan 02 added checkDupe.

Pagination per CONTEXT.md discretion: offset-based, 50 QSOs per page. "Load more" on scroll or page button.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server-Side Search + Update Endpoints</name>
  <read_first>
    @internal/handler/qso.go (current ListQSOs handler — needs ?search= param added)
    @internal/model/qso.go (QSO struct, CreateQSOInput — reuse for update)
    @.planning/phases/01-core-logger/01-RESEARCH.md (QSO schema, chi router URL params with chi.URLParam)
  </read_first>
  <action>
    Step 1 — Extend ListQSOs in internal/handler/qso.go:
    Add ?search= query parameter reading. If search is non-empty, add WHERE callsign LIKE ? || '%' to the SQL query (prefix match, parameterized). Keep existing ?limit= and ?offset= params. Order by timestamp DESC. Return total count via a separate COUNT query or include in response. Use parameterized queries for ALL user-supplied values — never string-concatenate.

    Step 2 — Add UpdateQSO handler in internal/handler/qso.go:
    New function UpdateQSO(db *sql.DB, w, r). Extract :id from URL using chi.URLParam(r, "id"). Parse id to int64 with strconv.ParseInt. Decode request body into model.CreateQSOInput (reuse struct). Validate required fields same as create. Recalculate points via qso.CalculatePoints(mode, isDupe from existing record). Execute UPDATE qsos SET callsign=?, band=?, mode=?, recv_exchange=?, sent_exchange=?, operator=?, points=? WHERE id=?. Use parameterized query. If no rows affected (wrong id), return 404. Return updated QSO as JSON.

    Step 3 — Wire new routes in main.go:
    Add chi route: r.Put("/{id}", UpdateQSOHandler) inside the /api/qso route group. Ensure URL param {id} is captured by chi.

    Step 4 — Verify:
    Use curl: POST a QSO (get id=1), then PUT /api/qso/1 with modified callsign, verify response has new callsign. GET /api/qso?search=K1 verifies prefix search. GET /api/qso?limit=10&offset=0 verifies pagination.
  </action>
  <verify>
    <automated>curl -s -X PUT http://localhost:8080/api/qso/1 -H 'Content-Type: application/json' -d '{"callsign":"K1MOD","band":"20M","mode":"SSB","recv_exchange":"2A NH"}' | grep -c '"K1MOD"' && curl -s 'http://localhost:8080/api/qso?search=K1M' | grep -c '"K1MOD"'</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/qso?search=K1 returns only QSOs with callsign starting with "K1" (case-insensitive LIKE)
    - GET /api/qso?limit=10&offset=20 returns correct paginated slice
    - PUT /api/qso/:id with valid JSON returns 200 with updated QSO object and recalculated points
    - PUT /api/qso/:id with missing required fields returns 400
    - PUT /api/qso/99999 (nonexistent) returns 404
    - All SQL uses parameterized queries — no string concatenation with user input
  </acceptance_criteria>
  <done>Search by callsign and QSO update endpoints functional, properly validated, and wired into chi router</done>
</task>

<task type="auto">
  <name>Task 2: Search UI + Inline Edit + Pagination in LogTable</name>
  <read_first>
    @frontend/src/lib/components/LogTable.svelte (current read-only table — needs inline edit, pagination, search)
    @frontend/src/lib/api.js (current API functions — needs updateQso, searchQsos added)
    @.planning/phases/01-core-logger/01-CONTEXT.md (D-05 inline editing spec, pagination discretion)
  </read_first>
  <action>
    Step 1 — Extend frontend/src/lib/api.js:
    Add export async function updateQso(id, data): fetch PUT to /api/qso/${id} with JSON body, return parsed response.
    Add export async function searchQsos(query, limit=50, offset=0): fetch GET /api/qso?search=${query}&limit=${limit}&offset=${offset}, return parsed JSON array.

    Step 2 — Add search input to LogTable.svelte:
    New text input above the table with placeholder "Search callsign...". On input (debounced 300ms), call searchQsos and update displayed rows. When search is empty, show default fetchQsos results.

    Step 3 — Add inline editing per D-05:
    Track editingId with $state (null when not editing). When a row is clicked (not the search input), set editingId = qso.id. The clicked row transforms:
    - Display cells become: text input for callsign, select dropdown for band (9 options), select for mode (7 options), text input for exchange.
    - Action buttons appear: [Save] and [Cancel].
    - On Save: call updateQso(id, {callsign, band, mode, recv_exchange}), on success set editingId=null and refresh table.
    - On Cancel: set editingId=null, restore original values.
    - Visual cue: edited row gets a highlighted background/border.

    Step 4 — Add pagination:
    Track currentOffset with $state (starting at 0). Track hasMore flag. Add "Load More" button at bottom of table. On click: increment offset by 50, call fetchQsos or searchQsos with new offset, append results. Hide button when hasMore is false (fewer than 50 results returned). Per discretion: 50 QSOs per page, offset-based.

    Step 5 — Verify:
    Submit a few QSOs. Click a row → inputs appear with current values. Change callsign, click Save → row returns to display with new callsign. Click Cancel → row returns unchanged. Type in search → table filters. Click "Load More" → more rows appear.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create frontend/src/lib/components/LogTable.test.js first</automated>
    <human-check>Open http://localhost:8080. Submit 3 QSOs. Click a row → verify inline edit mode with inputs for callsign, band, mode, exchange, and Save/Cancel buttons. Change callsign, Save → verify updated in table. Type in search box → verify filtering. Verify "Load More" pagination works when >50 QSOs exist.</human-check>
  </verify>
  <acceptance_criteria>
    - LogTable.svelte has search input above table that filters QSOs by callsign prefix (300ms debounce)
    - Clicking a QSO row transitions that row to edit mode: callsign input, band select, mode select, exchange input, Save and Cancel buttons
    - Save sends PUT /api/qso/:id and returns row to display mode with updated values per D-05
    - Cancel returns row to display mode preserving original values per D-05
    - Edited row is visually highlighted during edit mode
    - "Load More" button at bottom loads next 50 QSOs and appends to table
    - "Load More" hidden when fewer than 50 results returned
    - No modal, no separate edit page — inline only per D-05
  </acceptance_criteria>
  <done>Log table supports search, inline row editing with Save/Cancel, and offset-based pagination</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Search query from search input crosses via GET /api/qso?search= |
| client→API | Edit data crosses via PUT /api/qso/:id JSON body |
| client→API | QSO ID from URL path crosses via :id parameter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | GET /api/qso search parameter | mitigate | Use parameterized LIKE query with ? placeholder for search term; never string-concatenate user input into SQL |
| T-04-02 | Tampering | PUT /api/qso/:id handler | mitigate | Decode into explicit struct; validate required fields; use parameterized UPDATE with ? placeholders |
| T-04-03 | Information Disclosure | Searchable QSO log | accept | LAN-only trusted environment; search is essential feature; no sensitive data in QSO records |
| T-04-04 | Tampering | QSO ID in URL path | mitigate | Parse id with strconv.ParseInt; if invalid, return 400; use ? placeholder for id in SQL |
</threat_model>

<verification>
End-to-end: Submit 3 QSOs via form. Use search to filter by callsign. Click a row, edit callsign, Save. Verify persistence on refresh. Verify "Load More" pagination works.
</verification>

<success_criteria>
Operators can search QSOs, click to edit inline, and paginate through the log. Zero modals. All editing happens in the table row per D-05.
</success_criteria>

<output>
Create .planning/phases/01-core-logger/01-04-SUMMARY.md when done
</output>
