---
phase: 03-offline-resilience-polish
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - frontend/src/app.css
  - frontend/src/routes/+page.svelte
  - frontend/src/lib/components/QsoEntryForm.svelte
  - frontend/src/lib/components/LogTable.svelte
  - frontend/src/lib/components/StatsBar.svelte
  - frontend/src/lib/components/StationConfig.svelte
  - frontend/src/lib/components/OperatorSelector.svelte
autonomous: true
requirements: [UX-02]

must_haves:
  truths:
    - "Toggle button in header bar switches between light and dark theme instantly"
    - "All components (form, table, stats, config, operator selector) render correctly in both themes"
    - "No hardcoded hex/rgb colors remain in any component <style> block after migration"
    - "Theme persists across page refreshes (localStorage)"
    - "First visit with no localStorage preference respects OS dark mode setting"
    - "Manual toggle overrides and persists — subsequent visits use stored preference"
  artifacts:
    - path: "frontend/src/app.css"
      provides: ":root CSS custom properties (14 variables) + [data-theme='dark'] override block"
      contains: "--color-primary"
      contains: "[data-theme='dark']"
    - path: "frontend/src/routes/+page.svelte"
      provides: "Theme toggle button in header-left between title and WS indicator"
      contains: "toggleTheme"
      contains: "prefers-color-scheme"
  key_links:
    - from: "frontend/src/routes/+page.svelte theme toggle"
      to: "document.body [data-theme] attribute"
      via: "applyTheme() sets document.body.setAttribute('data-theme', theme.value)"
    - from: "All component <style> blocks"
      to: "frontend/src/app.css :root variables"
      via: "var(--color-*) references — CSS cascade from app.css"
---

<objective>
Deliver dark mode theme via CSS custom properties with a manual toggle in the header bar. All 14 hardcoded colors across 5 Svelte components are migrated to CSS variables in a single pass (per D-16). The toggle persists in localStorage and respects OS preference on first visit.

Purpose: Field Day operators work in dimly lit tents at night. A bright white screen ruins night vision and distracts other operators. Dark mode is a contest-operations requirement, not a cosmetic preference.
Output: Full dark mode with one-click toggle, zero hardcoded colors remaining
</objective>

<execution_context>
@/home/jeremy/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/jeremy/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-offline-resilience-polish/03-CONTEXT.md (D-13 through D-16)
@.planning/phases/03-offline-resilience-polish/03-RESEARCH.md (## Pattern 4 CSS Custom Properties & Dark Mode, ## Color palette audit)

# All component files with hardcoded colors
@frontend/src/app.css
@frontend/src/routes/+page.svelte
@frontend/src/lib/components/QsoEntryForm.svelte
@frontend/src/lib/components/LogTable.svelte
@frontend/src/lib/components/StatsBar.svelte
@frontend/src/lib/components/StationConfig.svelte
@frontend/src/lib/components/OperatorSelector.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS Custom Properties + Dark Palette + Toggle Button</name>
  <files>
frontend/src/app.css
frontend/src/routes/+page.svelte
  </files>
  <read_first>
@frontend/src/app.css
@frontend/src/routes/+page.svelte
@.planning/phases/03-offline-resilience-polish/03-RESEARCH.md (## Pattern 4 CSS Custom Properties & Dark Mode, ## Color palette audit — 14 variables table)
@.planning/phases/03-offline-resilience-polish/03-CONTEXT.md (D-13, D-14, D-15)
  </read_first>
  <action>
**D-13, D-14, D-15 — per CONTEXT.md decisions and RESEARCH.md § Pattern 4:**

1. **frontend/src/app.css**: Replace the existing `html, body` block's hardcoded colors with CSS custom properties:
   - After the reset block `(*, *::before, *::after)` and before the `html, body` block, insert the full CSS custom property definitions from RESEARCH.md § Pattern 4 (Color palette audit table). Copy the exact 14-variable `:root` block and the full `[data-theme='dark']` override block from RESEARCH.md lines 354-386.
   - Modify the `html, body` block: change `background: #f5f5f5;` to `background: var(--color-bg);` and `color: #222;` to `color: var(--color-text);`
   - The `:root` block defines: --color-primary, --color-accent, --color-success, --color-danger, --color-bg, --color-surface, --color-text, --color-text-secondary, --color-text-muted, --color-highlight, --color-border-light, --color-border, --color-border-strong, --color-bg-alt
   - The `[data-theme='dark']` block overrides all 14 with the dark palette values from RESEARCH.md

2. **frontend/src/routes/+page.svelte**: Add theme toggle logic and toggle button (D-14, D-15):
   - Add `<script>` imports and theme state management:
     ```
     let theme = $state('light');
     
     function initTheme() {
       const stored = localStorage.getItem('fdlogger_theme');
       if (stored === 'light' || stored === 'dark') {
         theme = stored;
       } else if (window.matchMedia('(prefers-color-scheme: dark)').matches) {
         theme = 'dark';
       }
       applyTheme();
     }
     
     function toggleTheme() {
       theme = theme === 'light' ? 'dark' : 'light';
       localStorage.setItem('fdlogger_theme', theme);
       applyTheme();
     }
     
     function applyTheme() {
       document.body.setAttribute('data-theme', theme);
     }
     ```
   - Call `initTheme()` at the top of the existing `onMount` (or add a new `onMount` call)
   - In the header bar, inside `<div class="header-left">`, BEFORE the `<h1 class="title">FD Logger</h1>`, add the toggle button:
     ```
     <button class="theme-toggle" onclick={toggleTheme} aria-label="Toggle dark mode">
       {theme === 'light' ? '☀' : '☾'}
     </button>
     ```
   - Per D-14: "Toggle button placed in the header bar, far left — between the 'FD Logger' title and the WS connection status indicator." The button goes first in header-left, then title h1, then the queue count / ws-status spans.
   - Add CSS for `.theme-toggle`: `background: none; border: 1px solid rgba(255,255,255,0.3); border-radius: 4px; color: #fff; font-size: 16px; cursor: pointer; padding: 2px 6px; min-width: 36px; min-height: 36px;`
   - Hover: `background: rgba(255,255,255,0.15);`
   - The existing header bar uses `background: #1a3a6b` and `color: #fff` — the toggle button inherits this color context. Do NOT change header.css colors in this task — the color migration (Task 2) will handle that.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -3</automated>
    <automated>grep -c '\-\-color-primary' frontend/src/app.css | xargs -I{} test {} -ge 2</automated>
  </verify>
  <done>
- `app.css` defines 14 CSS custom properties on `:root` with light theme values
- `app.css` defines `[data-theme='dark']` override block with 14 dark theme values
- `+page.svelte` has toggle button with sun/moon icon in header-left, before the title h1
- Toggle sets `data-theme` attribute on `<body>` and persists `fdlogger_theme` in localStorage
- First visit (no localStorage) checks `prefers-color-scheme: dark` and applies accordingly
- `npm run build` succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Color Migration — Replace Hardcoded Colors with CSS Variables</name>
  <files>
frontend/src/routes/+page.svelte
frontend/src/lib/components/QsoEntryForm.svelte
frontend/src/lib/components/LogTable.svelte
frontend/src/lib/components/StatsBar.svelte
frontend/src/lib/components/StationConfig.svelte
frontend/src/lib/components/OperatorSelector.svelte
  </files>
  <read_first>
@frontend/src/routes/+page.svelte
@frontend/src/lib/components/QsoEntryForm.svelte
@frontend/src/lib/components/LogTable.svelte
@frontend/src/lib/components/StatsBar.svelte
@frontend/src/lib/components/StationConfig.svelte
@frontend/src/lib/components/OperatorSelector.svelte
@.planning/phases/03-offline-resilience-polish/03-RESEARCH.md (## Pattern 4 — Color palette audit table with exact current-value→variable mapping)
  </read_first>
  <action>
**D-16 — per CONTEXT.md: "All hardcoded colors across every component migrated to CSS variables in a single pass."**

Use the color→variable mapping from RESEARCH.md § Pattern 4 (Color palette audit table, lines 334-350). For each component file, replace every hardcoded color value in `<style>` blocks with the corresponding `var(--color-*)` reference. Mapping:

| Find (hardcoded) | Replace with |
|---|---|
| `#1a3a6b` | `var(--color-primary)` |
| `#2266cc` | `var(--color-accent)` |
| `#1a7a1a` | `var(--color-success)` |
| `#cc3300` | `var(--color-danger)` |
| `#f5f5f5` | `var(--color-bg)` |
| `#ffffff`, `#fff` | `var(--color-surface)` |
| `#222222`, `#222` | `var(--color-text)` |
| `#555555`, `#555` | `var(--color-text-secondary)` |
| `#888888`, `#888` | `var(--color-text-muted)` |
| `#e8f0fe` | `var(--color-highlight)` |
| `#c4d7f2` | `var(--color-border-light)` |
| `#cccccc`, `#ccc` | `var(--color-border)` |
| `#dddddd`, `#ddd` | `var(--color-border-strong)` |
| `#f0f0f0` | `var(--color-bg-alt)` |

Additional matches not in the 14-variable palette but found in codebase:
| Find | Replace with closest variable |
|---|---|
| `#fafafa` (LogTable alternating rows) | `var(--color-bg)` (light gray bg is semantically same as page bg) |
| `#e8e8e8` (LogTable th background) | `var(--color-bg-alt)` |
| `#eee` (LogTable td border) | `var(--color-border)` |
| `#f8fafd` (StationConfig input bg) | `var(--color-bg)` |
| `#1a52a3` (QsoEntryForm button hover) | `var(--color-primary)` (darkened primary — same color family as #1a3a6b → use primary) |
| `#2a4f8f` (StationConfig save hover) | `var(--color-primary)` |
| `#156315` (LogTable save hover) | `var(--color-success)` (darkened success) |
| `#fffde7` (LogTable edit row bg) | `var(--color-highlight)` (warm highlight — closest semantic match) |
| `#f0c040` (LogTable edit row border) | Keep as-is (unique edit indicator color — add note that this is intentional) |
| `#f8f8f8` (StatsBar breakdown panel) | `var(--color-bg)` |
| `#aaa` (StatsBar toggle border) | `var(--color-text-muted)` |
| `#777` (StatsBar unit) | `var(--color-text-secondary)` |
| `rgba(26,58,107,0.2)` (StationConfig focus shadow) | Replace with `color-mix(in srgb, var(--color-primary) 20%, transparent)` for the shadow, or use a simpler approach: keep rgba but define a separate `--color-primary-rgb: 26, 58, 107;` variable for the focus shadow. Simpler: `box-shadow: 0 0 0 2px var(--color-border-light);` since the shadow serves the same purpose as border highlighting.

**Component by component changes:**

1. **+page.svelte** `<style>`:
   - `.header-bar` `background: #1a3a6b;` → `background: var(--color-primary);`
   - `.header-bar` `color: #fff;` → `color: var(--color-surface);`
   - `.ws-status.online` `color: #1a7a1a;` → `color: var(--color-success);`
   - `.ws-status.offline` `color: #cc3300;` → `color: var(--color-danger);`
   - `.export-btn` `border: 2px solid #fff;` → `border: 2px solid var(--color-surface);`
   - `.export-btn` `color: #fff;` → `color: var(--color-surface);`
   - `.export-btn:hover` `background: rgba(255,255,255,0.15);` → keep rgba — transparent overlay on variable bg

2. **QsoEntryForm.svelte** `<style>`:
   - `.qso-form` `background: #fff;` → `background: var(--color-surface);`
   - `.qso-form` `border-bottom: 2px solid #ddd;` → `border-bottom: 2px solid var(--color-border-strong);`
   - `input, select` `border: 2px solid #ccc;` → `border: 2px solid var(--color-border);`
   - `input, select` `background: #fff;` → `background: var(--color-surface);`
   - `input:focus, select:focus` `border-color: #2266cc;` → `border-color: var(--color-accent);`
   - `.dupe-warning` `color: #cc3300;` → `color: var(--color-danger);`
   - `button` `background: #2266cc;` → `background: var(--color-accent);`
   - `button` `color: #fff;` → `color: var(--color-surface);`
   - `button:hover:not(:disabled)` `background: #1a52a3;` → `background: var(--color-primary);`

3. **LogTable.svelte** `<style>`:
   - `.search-bar` `background: #f0f0f0;` → `background: var(--color-bg-alt);`
   - `.search-bar` `border-bottom: 1px solid #ddd;` → `border-bottom: 1px solid var(--color-border-strong);`
   - `.search-input` `border: 2px solid #ccc;` → `border: 2px solid var(--color-border);`
   - `.search-input:focus` `border-color: #2266cc;` → `border-color: var(--color-accent);`
   - `.log-table th` `background: #e8e8e8;` → `background: var(--color-bg-alt);`
   - `.log-table th` `color: #555;` → `color: var(--color-text-secondary);`
   - `.log-table th` `border-bottom: 2px solid #ccc;` → `border-bottom: 2px solid var(--color-border);`
   - `.log-table td` `border-bottom: 1px solid #eee;` → `border-bottom: 1px solid var(--color-border);`
   - `.log-table tbody tr:nth-child(even)` `background: #fafafa;` → `background: var(--color-bg);`
   - `.qso-row:hover` `background: #e8f0fe !important;` → `background: var(--color-highlight) !important;`
   - `.edit-row` `background: #fffde7 !important;` → `background: var(--color-highlight) !important;` (warm highlight → closest variable; the `border-left: 3px solid #f0c040;` stays as-is)
   - `.edit-input, .edit-select` `border: 2px solid #2266cc;` → `border: 2px solid var(--color-accent);`
   - `.btn-save` `background: #1a7a1a;` → `background: var(--color-success);`
   - `.btn-save:hover` `background: #156315;` → `background: var(--color-success);` (opacity/filter alternative: add `filter: brightness(0.9);` on hover instead of separate color)
   - `.btn-cancel` `border: 1px solid #ccc;` → `border: 1px solid var(--color-border);`
   - `.btn-cancel` `background: #fff;` → `background: var(--color-surface);`
   - `.btn-cancel:hover` `background: #f0f0f0;` → `background: var(--color-bg-alt);`
   - `.empty-state` `color: #888;` → `color: var(--color-text-muted);`
   - `.load-more button` `border: 2px solid #2266cc;` → `border: 2px solid var(--color-accent);`
   - `.load-more button` `background: #fff;` → `background: var(--color-surface);`
   - `.load-more button` `color: #2266cc;` → `color: var(--color-accent);`
   - `.load-more button:hover` `background: #e8f0fe;` → `background: var(--color-highlight);`

4. **StatsBar.svelte** `<style>`:
   - `.stats-bar` `background: #e8f0fe;` → `background: var(--color-highlight);`
   - `.stats-bar` `border-bottom: 1px solid #c4d7f2;` → `border-bottom: 1px solid var(--color-border-light);`
   - `.stat-label` `color: #555;` → `color: var(--color-text-secondary);`
   - `.stat-value` `color: #1a3a6b;` → `color: var(--color-primary);`
   - `.stat-unit` `color: #777;` → `color: var(--color-text-secondary);`
   - `.rate .stat-value` `color: #cc3300;` → `color: var(--color-danger);`
   - `.score .stat-value` `color: #1a7a1a;` → `color: var(--color-success);`
   - `.breakdown-toggle` `border: 1px solid #aaa;` → `border: 1px solid var(--color-text-muted);`
   - `.breakdown-toggle` `background: #fff;` → `background: var(--color-surface);`
   - `.breakdown-toggle:hover` `background: #f0f0f0;` → `background: var(--color-bg-alt);`
   - `.breakdown-panel` `background: #f8f8f8;` → `background: var(--color-bg);`
   - `.breakdown-panel` `border-bottom: 1px solid #ddd;` → `border-bottom: 1px solid var(--color-border-strong);`
   - `.breakdown-table th` `color: #555;` → `color: var(--color-text-secondary);`
   - `.empty-breakdown` `color: #888;` → `color: var(--color-text-muted);`

5. **StationConfig.svelte** `<style>`:
   - `.config-toggle` `border: 2px solid #fff;` → `border: 2px solid var(--color-surface);`
   - `.config-toggle` `color: #fff;` → `color: var(--color-surface);`
   - `.config-panel` `background: #fff;` → `background: var(--color-surface);`
   - `.config-panel` `border: 1px solid #c4d7f2;` → `border: 1px solid var(--color-border-light);`
   - `.field label` `color: #555;` → `color: var(--color-text-secondary);`
   - `.field input, .field select` `border: 1px solid #c4d7f2;` → `border: 1px solid var(--color-border-light);`
   - `.field input, .field select` `background: #f8fafd;` → `background: var(--color-bg);`
   - `.field input, .field select` `color: #1a3a6b;` → `color: var(--color-primary);`
   - `.field input:focus, .field select:focus` `border-color: #1a3a6b;` → `border-color: var(--color-primary);`
   - `.field input:focus, .field select:focus` `box-shadow: 0 0 0 2px rgba(26,58,107,0.2);` → `box-shadow: 0 0 0 2px var(--color-border-light);`
   - `.save-btn` `background: #1a3a6b;` → `background: var(--color-primary);`
   - `.save-btn` `color: #fff;` → `color: var(--color-surface);`
   - `.save-btn:hover` `background: #2a4f8f;` → `background: var(--color-primary); filter: brightness(1.15);`
   - `.saved-msg` `color: #1a7a1a;` → `color: var(--color-success);`

6. **OperatorSelector.svelte** `<style>`:
   - `.operator-selector input` `border: 1px solid #ccc;` → `border: 1px solid var(--color-border);`
   - Note: The OperatorSelector is inside the header bar (dark background). Text color for `.operator-selector` and `.operator-selector input` is not explicitly set — it inherits from the header's `color: var(--color-surface)`. Input text may need explicit `color: var(--color-text)` to be readable. Check after migration.

**After all replacements, verify no hardcoded colors remain:**
- Run `grep -rn '#[0-9a-fA-F]\{3,6\}' frontend/src/lib/components/ frontend/src/routes/ frontend/src/app.css` — the only remaining hex colors should be in comments, the `color-mix()` function, or intentional exceptions (e.g., `#f0c040` edit row border)
- Run `grep -rn 'rgba(' frontend/src/lib/components/ frontend/src/routes/ frontend/src/app.css` — only transparent overlays on variable backgrounds should remain (e.g., `rgba(255,255,255,0.15)` for hover effects)
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -3</automated>
    <automated>REMAINING=$(grep -rn '#[0-9a-fA-F]\{3,6\}' frontend/src/lib/components/ frontend/src/routes/ frontend/src/app.css 2>/dev/null | grep -v 'color-mix' | grep -v 'f0c040' | grep -v '//' | wc -l); test $REMAINING -le 5</automated>
  </verify>
  <done>
- No hardcoded hex colors remain in any component `<style>` block (except intentional exceptions: `#f0c040` edit border, `color-mix()` usage)
- CSS variables flow correctly: `app.css :root` → component `var(--color-*)` references
- Toggle button switches theme instantly (no page flash)
- Dark mode renders all 5 components with correct dark palette
- Light mode renders identically to pre-migration appearance (visual parity)
- `npm run build` succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → theme toggle | User preference stored in localStorage — read on page load to set `data-theme` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | localStorage fdlogger_theme | accept | Theme value is non-security-critical. Tampering only changes visual appearance for that browser. No PII or auth stored. |
| T-03-06 | Information Disclosure | CSS prefers-color-scheme | accept | OS-level preference is read-only by design. No information leaked beyond what the browser already exposes. |
</threat_model>

<verification>
## Phase-Level Verification

- **Visual parity:** Toggle to light mode, compare side-by-side with pre-migration screenshots — all colors match
- **Dark mode coverage:** Toggle to dark mode, scroll through all components, verify no white/light sections remain
- **Persistence:** Toggle dark mode, refresh page, verify dark mode persists
- **OS detection:** Clear localStorage (`localStorage.removeItem('fdlogger_theme')`), set OS to dark mode, refresh — verify dark mode activates
- **No flash:** Toggle and refresh — verify no flash of wrong theme on page load (data-theme is set before first paint due to initTheme in onMount)
</verification>

<success_criteria>
1. Toggle button ☀/☾ visible in header bar (far left, before title)
2. Clicking toggle switches all component colors between light and dark palettes
3. Dark mode renders all components without any white/light sections
4. Theme persists across page refreshes (localStorage key `fdlogger_theme`)
5. First visit with OS dark mode auto-activates dark theme
6. Zero hardcoded hex colors remain in component `<style>` blocks (verified by grep)
</success_criteria>

<output>
Create `.planning/phases/03-offline-resilience-polish/03-03-SUMMARY.md` when done
</output>
