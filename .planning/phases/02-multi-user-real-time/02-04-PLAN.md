---
phase: 02-multi-user-real-time
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - internal/cabrillo/cabrillo.go
  - internal/cabrillo/cabrillo_test.go
  - internal/handler/export.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "Cabrillo export reads station callsign, class, ARRL section, and power level from station_config table"
    - "Cabrillo export falls back to N0CALL/1D/EMA/LOW defaults when no config exists"
    - "Export handler uses real station callsign in the Content-Disposition filename"
    - "Generated Cabrillo headers match actual configured station values"
  artifacts:
    - path: internal/cabrillo/cabrillo.go
      provides: "Updated Generate() that reads station_config table for header metadata"
      contains: "station_config"
    - path: internal/cabrillo/cabrillo_test.go
      provides: "Updated tests verifying real station config is used in output"
      contains: "station_config"
    - path: internal/handler/export.go
      provides: "Updated ExportCabrillo that uses config callsign for filename"
      contains: "station_config"
  key_links:
    - from: internal/cabrillo/cabrillo.go
      to: station_config table
      via: "db.QueryRow -> station_config WHERE id = 1"
      pattern: "station_config"
    - from: internal/handler/export.go
      to: cabrillo.Generate
      via: "cabrillo.Generate(db)"
      pattern: "Generate"
---

<objective>
Replace hardcoded "N0CALL" in Cabrillo export with real station configuration from the station_config table. This plan updates the Cabrillo generator to read the station callsign, class, ARRL section, and power level from the database (set by Plan 02-01), and updates the export handler to use the real callsign in the downloaded filename.

Purpose: The Phase 1 Cabrillo export hardcoded N0CALL as the callsign and NH as the section. Phase 2 introduced a configurable station_config table (Plan 02-01). This plan connects the two — so the exported Cabrillo file reflects the actual station's configured values.

Output: Cabrillo export generates headers with real station data. Filename is `{callsign}_field_day.cbr` instead of `n0call_field_day.cbr`.
</objective>

<execution_context>
@/home/jeremy/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/jeremy/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-multi-user-real-time/02-RESEARCH.md — Pattern 4 (station_config schema), Open Question 3 (no override params)
@.planning/STATE.md

# Existing code this plan builds on:
@internal/cabrillo/cabrillo.go — current Generate(): hardcodes CALLSIGN: N0CALL (line 39), ARRL-SECTION: NH (line 41), CATEGORY-POWER: LOW (line 43)
@internal/cabrillo/cabrillo_test.go — existing tests verify hardcoded "N0CALL" in output (line 52: strings.Contains result, "CALLSIGN: N0CALL")
@internal/handler/export.go — current ExportCabrillo: calls cabrillo.Generate(db), hardcodes filename "n0call_field_day.cbr" (line 18)
@internal/model/config.go — StationConfig struct, DefaultStationConfig() (created in Plan 02-01)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update cabrillo.Generate() to read station_config + update tests</name>
  <read_first>
@internal/cabrillo/cabrillo.go — current Generate() implementation: reads from qsos table only, hardcodes N0CALL/NH/LOW in header lines 37-44
@internal/cabrillo/cabrillo_test.go — current tests: setupCabrilloTestDB only creates qsos table; TestGenerate_EmptyDB expects CALLSIGN: N0CALL
@internal/db/schema.sql — station_config table schema (created in Plan 02-01): CREATE TABLE station_config (id, callsign, class, arrl_section, transmitter_count, power_level, updated_at)
  </read_first>
  <files>internal/cabrillo/cabrillo.go, internal/cabrillo/cabrillo_test.go</files>
  <behavior>
    - Test 1: When station_config table exists with callsign=K1ABC, class=2A, section=SNJ, power=HIGH, Cabrillo headers contain CALLSIGN: K1ABC, ARRL-SECTION: SNJ, CATEGORY-POWER: HIGH
    - Test 2: CLASS tag in Cabrillo reflects the configured class (e.g., CATEGORY-CLASS: 2A)
    - Test 3: When station_config table does NOT exist (or is empty), Generate() falls back to defaults (N0CALL, NH, LOW) — backward compatible
    - Test 4: When station_config row exists but class is empty string, fallback to "1D" default
    - Test 5: Existing QSO data is still correctly included in Cabrillo output (the change is header-only)
    - Test 6: CLAIMED-SCORE and QSO lines are unaffected by the config change
  </behavior>
  <action>
Modify internal/cabrillo/cabrillo.go — Generate() function:

1. Before writing headers (before current line 36), add a config lookup:
```
var callsign, class, section, power string
callsign = "N0CALL"
class = "1D"
section = "NH"
power = "LOW"

err := db.QueryRow("SELECT callsign, class, arrl_section, power_level FROM station_config WHERE id = 1").Scan(&callsign, &class, &section, &power)
if err == sql.ErrNoRows {
    // use defaults (already set above)
} else if err != nil {
    // log warning but continue with defaults — don't fail export for config read error
    // return empty string with error if no way to log? Actually, just use defaults silently — database error shouldn't block export
}
```

2. Replace hardcoded header lines:
- Line 39 `CALLSIGN: N0CALL` → `fmt.Sprintf("CALLSIGN: %s\n", callsign)`
- Line 40 `ARRL-SECTION: NH` → `fmt.Sprintf("ARRL-SECTION: %s\n", section)`
- Line 41 `CATEGORY-OPERATOR: SINGLE-OP` → keep as-is (always SINGLE-OP for Field Day)
- Line 42 `CATEGORY-POWER: LOW` → `fmt.Sprintf("CATEGORY-POWER: %s\n", power)`
- Add after line 44: `fmt.Sprintf("CATEGORY-CLASS: %s\n", class)` — new header line for station class

3. Add import "database/sql" if not already present (it should be from existing code).

Modify internal/cabrillo/cabrillo_test.go:

1. Update setupCabrilloTestDB helper function to ALSO create the station_config table:
```
_, err = db.Exec(`CREATE TABLE IF NOT EXISTS station_config (
    id INTEGER PRIMARY KEY CHECK (id = 1),
    callsign TEXT NOT NULL DEFAULT 'N0CALL',
    class TEXT NOT NULL DEFAULT '1D',
    arrl_section TEXT NOT NULL DEFAULT 'EMA',
    transmitter_count INTEGER NOT NULL DEFAULT 1,
    power_level TEXT NOT NULL DEFAULT 'LOW',
    updated_at TEXT NOT NULL DEFAULT (datetime('now'))
)`)
```

2. Update TestGenerate_EmptyDB to check for CALLSIGN: N0CALL (default) — this test should still pass since no config is inserted.

3. Add new test TestGenerate_WithConfig: insert a station config row (INSERT OR REPLACE with callsign=K1ABC, class=2A, arrl_section=SNJ, power_level=HIGH), then call Generate() and verify headers contain CALLSIGN: K1ABC, ARRL-SECTION: SNJ, CATEGORY-POWER: HIGH, CATEGORY-CLASS: 2A.

4. Add new test TestGenerate_ConfigFallback: drop station_config table (or use empty DB without config row), call Generate(), verify default headers (CALLSIGN: N0CALL, ARRL-SECTION: NH).
  </action>
  <verify>
    <automated>go test ./internal/cabrillo/ -v -count=1</automated>
  </verify>
  <done>All 6 behavior tests pass. Cabrillo Generate() reads station_config for CALLSIGN, ARRL-SECTION, CATEGORY-POWER, CATEGORY-CLASS headers. Falls back to defaults when config not found.</done>
</task>

<task type="auto">
  <name>Task 2: Update ExportCabrillo handler to use real callsign in filename</name>
  <read_first>
@internal/handler/export.go — current ExportCabrillo: line 18 hardcodes filename "n0call_field_day.cbr"
@internal/cabrillo/cabrillo.go — Generate() now takes *sql.DB and reads station_config internally
@internal/model/config.go — StationConfig struct for reference
  </read_first>
  <files>internal/handler/export.go</files>
  <behavior>
    - Test 1: When station_config has callsign=K1ABC, Content-Disposition header is attachment; filename="k1abc_field_day.cbr"
    - Test 2: When station_config is empty, filename falls back to "n0call_field_day.cbr"
    - Test 3: Callsign is lowercased in filename
    - Test 4: Export handler returns 200 with text/plain content type
  </behavior>
  <action>
Modify internal/handler/export.go — ExportCabrillo function:

1. Before calling cabrillo.Generate(db), read the callsign from station_config:
```
callsign := "n0call"
var c string
if err := db.QueryRow("SELECT callsign FROM station_config WHERE id = 1").Scan(&c); err == nil && c != "" {
    callsign = strings.ToLower(c)
}
```

2. Add import for "strings" (for ToLower).

3. Change line 18 filename from:
`w.Header().Set("Content-Disposition", "attachment; filename=\"n0call_field_day.cbr\"")`
to:
`w.Header().Set("Content-Disposition", fmt.Sprintf("attachment; filename=\"%s_field_day.cbr\"", callsign))`

4. Add import for "fmt" if not already present.

Keep everything else the same — cabrillo.Generate(db) now reads station_config internally (updated in Task 1), so the Cabrillo content will already have the correct headers.
  </action>
  <verify>
    <automated>go test ./internal/handler/ -v -count=1 -run "TestExportCabrillo" 2>/dev/null || go test ./internal/cabrillo/ -v -count=1</automated>
    <automated>go build ./...</automated>
  </verify>
  <done>ExportCabrillo handler uses real station callsign (lowercased) in Content-Disposition filename. Falls back to "n0call_field_day.cbr" when no config. go build succeeds. Cabrillo content and filename both reflect real config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server GET /api/export/cabrillo | Any LAN client can request a Cabrillo export — same trust model as all other endpoints |
| Database → Cabrillo generator | Config values read from station_config flow into file header — no user input traverses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Tampering | SQL injection in station_config read query | mitigate | Parameterized query `SELECT ... FROM station_config WHERE id = 1` uses no user input. Call sign value comes from DB, not request params. |
| T-02-15 | Information Disclosure | Station callsign in exported filename | accept | Intended behavior — Cabrillo files are submitted to ARRL with callsign in filename. Callsign is public information. |
</threat_model>

<verification>
Overall checks after this plan:
- `go test ./internal/cabrillo/ -v -count=1` passes all tests (existing + new config tests)
- `go build ./...` compiles clean
- Manual: Set station config via PUT /api/station-config, then GET /api/export/cabrillo — verify headers match configured values
- Manual: Download filename matches configured callsign
</verification>

<success_criteria>
- [ ] cabrillo.Generate() reads callsign/class/section/power from station_config
- [ ] Falls back to defaults (N0CALL, NH, LOW) when config table empty
- [ ] CATEGORY-CLASS header added to Cabrillo output
- [ ] Export handler uses real callsign in Content-Disposition filename
- [ ] All existing Cabrillo tests updated and passing
- [ ] New tests verify config-driven and fallback scenarios
</success_criteria>

<output>
Create `.planning/phases/02-multi-user-real-time/02-04-SUMMARY.md` when done
</output>
