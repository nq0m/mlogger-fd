---
phase: 04-field-day-features-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/db/schema.sql
  - internal/model/bonus.go
  - internal/handler/bonus.go
  - main.go
autonomous: true
requirements: [BON-01]

must_haves:
  truths:
    - "Bonus claims table exists in SQLite schema with bonus_id (TEXT PK), claimed (INTEGER), count (INTEGER), updated_at (TEXT)"
    - "GET /api/bonuses returns a JSON map of all 18 bonus items with claimed/count state, even when bonus_claims table is empty"
    - "PUT /api/bonuses accepts a JSON map, validates bonus_ids against the known 2026 ARRL list, and persists claims in a single transaction"
    - "Unknown bonus_ids in PUT body are silently ignored (no error, no insertion)"
    - "Bonus count values are validated: non-negative integers within max bounds per bonus type"
  artifacts:
    - path: "internal/db/schema.sql"
      provides: "bonus_claims table DDL"
      contains: "CREATE TABLE IF NOT EXISTS bonus_claims"
    - path: "internal/model/bonus.go"
      provides: "BonusItem struct, BonusClaim struct, DefaultBonuses constant, bonus points calculation, validation"
      exports: ["BonusItem", "BonusClaim", "DefaultBonuses", "ValidateBonusClaim", "CalculateBonusPoints"]
    - path: "internal/handler/bonus.go"
      provides: "GET/PUT /api/bonuses HTTP handlers"
      exports: ["GetBonuses", "PutBonuses"]
    - path: "main.go"
      provides: "Chi router wiring for /api/bonuses"
      contains: "r.Get(\"/bonuses\""
  key_links:
    - from: "main.go"
      to: "internal/handler/bonus.go"
      via: "chi router closure: handler.GetBonuses(database, w, r)"
      pattern: "GetBonuses\\(database"
    - from: "internal/handler/bonus.go"
      to: "internal/model/bonus.go"
      via: "import model package, use DefaultBonuses for validation"
      pattern: "model\\.DefaultBonuses"
    - from: "internal/handler/bonus.go"
      to: "internal/db/schema.sql"
      via: "INSERT OR REPLACE INTO bonus_claims"
      pattern: "bonus_claims"
---

<objective>
Create the Bonus Tracker backend: SQLite `bonus_claims` table, Go model layer with the hardcoded 2026 ARRL bonus list per D-01, REST API handler for GET/PUT `/api/bonuses` per D-04, and Chi router wiring in `main.go`.

Purpose: The bonus tracker API must exist before the frontend BonusTracker component can display or save bonus claims. This plan delivers the complete server-side CRUD for bonus claims — operators can fetch the full bonus list with claim state and save toggles/counts via the API.
Output: `bonus_claims` table in schema, `model/bonus.go` with data structures and validation, `handler/bonus.go` with GET/PUT handlers, `main.go` with new routes wired.
</objective>

<execution_context>
@/home/jeremy/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/jeremy/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-field-day-features-testing/04-CONTEXT.md
@.planning/phases/04-field-day-features-testing/04-RESEARCH.md
@.planning/phases/04-field-day-features-testing/04-PATTERNS.md

# Key pattern sources
@internal/db/schema.sql
@internal/model/config.go
@internal/handler/config.go
@main.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create bonus_claims schema + model layer</name>
  <files>internal/db/schema.sql, internal/model/bonus.go</files>
  <read_first>
    - internal/db/schema.sql (existing CREATE TABLE IF NOT EXISTS pattern for idempotent migration)
    - internal/model/config.go (DefaultStationConfig factory, ValidateStationConfig validation pattern, struct tags with json)
    - .planning/phases/04-field-day-features-testing/04-RESEARCH.md § "2026 ARRL Field Day Bonus Points List" (lines 407-434 — full bonus table with IDs, names, rule refs, points, counted vs boolean, max counts)
    - .planning/phases/04-field-day-features-testing/04-RESEARCH.md § "Bonus Claims SQLite Schema" (lines 437-448 — table DDL)
    - .planning/phases/04-field-day-features-testing/04-RESEARCH.md § "Scoring Integration Details" (lines 784-813 — bonus points calculation formula: sum of claimed * points per bonus type, with special cases for emergency_power, message_handling, youth, GOTA, web_submission, safety_officer, site_responsibilities)
    - .planning/phases/04-field-day-features-testing/04-PATTERNS.md § "Pattern 1: schema" (lines 31-86) and § "Pattern 2: model/bonus.go" (lines 89-173)
  </read_first>
  <behavior>
    - Test 1: schema.sql contains `CREATE TABLE IF NOT EXISTS bonus_claims` with bonus_id TEXT PRIMARY KEY, claimed INTEGER NOT NULL DEFAULT 0, count INTEGER NOT NULL DEFAULT 0, updated_at TEXT NOT NULL DEFAULT (datetime('now'))
    - Test 2: model.BonusItem struct has fields: ID, Name, RuleRef, Points (int), IsCounted (bool), MaxCount (int), DefaultCount (int) with json tags
    - Test 3: model.DefaultBonuses contains exactly 18 items matching the 2026 ARRL bonus list from RESEARCH.md table (lines 411-431)
    - Test 4: model.CalculateBonusPoints(claims map[string]BonusClaim) returns correct total for a known set of claimed bonuses
    - Test 5: model.ValidateBonusClaim validates bonus_id against DefaultBonuses, count is non-negative and ≤ MaxCount for counted bonuses
  </behavior>
  <action>
    per D-01: Append `CREATE TABLE IF NOT EXISTS bonus_claims` to schema.sql following the existing `CREATE TABLE IF NOT EXISTS` pattern from line 20 of schema.sql. Table columns: bonus_id TEXT PRIMARY KEY, claimed INTEGER NOT NULL DEFAULT 0, count INTEGER NOT NULL DEFAULT 0, updated_at TEXT NOT NULL DEFAULT (datetime('now')).

    per D-01, D-02: Create `internal/model/bonus.go` with:
    1. `BonusItem` struct — immutable metadata for one bonus type: ID, Name, RuleRef, Points (int), IsCounted (bool), MaxCount (int, 0 for boolean), DefaultCount (int, 0).
    2. `BonusClaim` struct — per-bonus mutable claim state: BonusID, Claimed (bool), Count (int), UpdatedAt (string, omitempty). JSON tags: `"bonus_id"`, `"claimed"`, `"count"`, `"updated_at,omitempty"`.
    3. `DefaultBonuses` — `[]BonusItem` constant slice with all 18 items from the RESEARCH.md table (lines 411-431): emergency_power (100/transmitter, counted, max 20), media_publicity (100, boolean), public_location (100, boolean), public_info_table (100, boolean), message_to_sm (100, boolean), message_handling (10/msg, counted, max 10), satellite_qso (100, boolean), alternate_power (100, boolean), w1aw_bulletin (100, boolean), educational_activity (100, boolean), official_visit (100, boolean), agency_visit (100, boolean), gota_bonus (5/QSO, counted, max=0 for unlimited + separate coach boolean), web_submission (50, boolean), youth_participation (20/participant, counted, max 5), social_media (100, boolean), safety_officer (100, boolean), site_responsibilities (50, boolean).
    4. `ValidateBonusClaim(bonusID string, claimed bool, count int) string` — validates bonus_id exists in DefaultBonuses (returns error if unknown), validates count is >=0 and <=MaxCount for counted items. Returns empty string on success, error message on failure. Follows ValidateStationConfig pattern from model/config.go lines 22-39.
    5. `CalculateBonusPoints(claims map[string]BonusClaim) int` — computes total bonus points by iterating claims, looking up DefaultBonuses by ID, and summing: for emergency_power: claimed * count * 100, for message_handling: claimed * min(count,10) * 10, for youth_participation: claimed * min(count,5) * 20, for gota_bonus: claimed * count * 5 + (claimed && count >= 10 ? 100 : 0), for web_submission: claimed * 50, for safety_officer: claimed * 100, for site_responsibilities: claimed * 50, all others: claimed * points from BonusItem. Missing bonus_id in claims = 0 points for that bonus.

    per ARRL rules section 7.3: The CalculateBonusPoints function follows the ARRL rule that bonus points are added after the multiplier (bonusPoints are separate from rawPoints * multiplier). This is documented in RESEARCH.md lines 484-488. The CONTEXT.md formula on line 97 is noted as contradictory — implement the ARRL-correct post-multiplier formula.
  </action>
  <verify>
    <automated>go test ./internal/model/ -run TestBonus -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'bonus_claims' internal/db/schema.sql` returns at least 1
    - `grep -c 'DefaultBonuses' internal/model/bonus.go` returns at least 1
    - `grep -c 'emergency_power' internal/model/bonus.go` returns at least 1 (from DefaultBonuses slice)
    - `grep -c 'CalculateBonusPoints' internal/model/bonus.go` returns at least 1
    - `grep -c 'ValidateBonusClaim' internal/model/bonus.go` returns at least 1
    - `grep -c 'BonusItem' internal/model/bonus.go` returns at least 1
    - BonusItem has json tags: `"id"`, `"name"`, `"rule_ref"`, `"points"`, `"is_counted"`, `"max_count,omitempty"`, `"default_count,omitempty"`
    - BonusClaim has json tags: `"bonus_id"`, `"claimed"`, `"count"`, `"updated_at,omitempty"`
    - DefaultBonuses has exactly 18 items (verify: count BonusItem{ occurrences)
    - `go test ./internal/model/ -run TestBonus -count=1` exits 0
  </acceptance_criteria>
  <done>Schema has bonus_claims table. model/bonus.go has BonusItem, BonusClaim, DefaultBonuses (18 items), ValidateBonusClaim, CalculateBonusPoints. All model tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create bonus API handler + wire routes in main.go</name>
  <files>internal/handler/bonus.go, main.go</files>
  <read_first>
    - internal/handler/config.go (GetStationConfig/PutStationConfig pattern: JSON encode/decode, error response format with map[string]string{"error": "..."}, INSERT OR REPLACE pattern)
    - internal/model/bonus.go (created in Task 1 — BonusClaim struct, DefaultBonuses, ValidateBonusClaim)
    - main.go (existing Chi router closure pattern at lines 43-74 — r.Get/r.Put closures wrapping handler functions)
    - .planning/phases/04-field-day-features-testing/04-RESEARCH.md § "API Request/Response Format" (lines 449-462 — JSON shape: map of bonus_id → {claimed, count})
    - .planning/phases/04-field-day-features-testing/04-PATTERNS.md § "Pattern 3: handler/bonus.go" (lines 176-262) and § "Pattern 7: main.go routes" (lines 478-538)
  </read_first>
  <behavior>
    - Test 1: GET /api/bonuses returns 200 with a JSON object containing all 18 bonus IDs as keys, each with {claimed: bool, count: int} when table has data
    - Test 2: GET /api/bonuses returns 200 with all 18 bonus IDs and claimed=false, count=0 when bonus_claims table is empty
    - Test 3: PUT /api/bonuses with valid JSON returns 200 and persisted claims, then GET returns the same claims
    - Test 4: PUT /api/bonuses with unknown bonus_id in body: handler silently ignores unknown keys (no 400, no insertion of invalid row)
    - Test 5: PUT /api/bonuses with invalid count (negative or > MaxCount) returns 400 with error message
    - Test 6: PUT /api/bonuses with malformed JSON returns 400 with `{"error": "invalid JSON"}`
  </behavior>
  <action>
    Create `internal/handler/bonus.go` with:
    
    per D-04: `GetBonuses(db *sql.DB, w http.ResponseWriter, r *http.Request)` — Queries all rows from bonus_claims table. Builds a `map[string]model.BonusClaim` response. If table has no rows, builds the map from model.DefaultBonuses with all claimed=false, count=0. Follows GetStationConfig pattern from handler/config.go lines 11-30: set Content-Type application/json, encode with json.NewEncoder, error responses use `map[string]string{"error": "..."}`.
    
    per D-02, D-04: `PutBonuses(db *sql.DB, w http.ResponseWriter, r *http.Request)` — Decodes JSON body into `map[string]model.BonusClaim`. Validates each entry: (1) bonus_id must exist in model.DefaultBonuses — unknown IDs are silently skipped (no error per security guidance). (2) calls model.ValidateBonusClaim for each — if any validation fails, returns 400 with the error message. On success, wraps all INSERT OR REPLACE statements in a transaction: `db.Exec("INSERT OR REPLACE INTO bonus_claims (bonus_id, claimed, count, updated_at) VALUES (?, ?, ?, datetime('now'))")`. Returns the full state as response (like PutStationConfig returns cfg).
    
    per security guidance (ASVS V5): Validate count is non-negative integer and within MaxCount per bonus type. Unknown bonus_ids are silently ignored (not inserted). This prevents bonus_id injection per RESEARCH.md known threat pattern.

    per D-04: In `main.go`, add two Chi route closures inside the existing `r.Route("/api", ...)` block (between the `/station-config` block at line 59 and `/sync` at line 62), following the exact closure pattern:
    ```go
    r.Get("/bonuses", func(w http.ResponseWriter, r *http.Request) {
        handler.GetBonuses(database, w, r)
    })
    r.Put("/bonuses", func(w http.ResponseWriter, r *http.Request) {
        handler.PutBonuses(database, w, r)
    })
    ```
    The handler package already imports via `"github.com/jeremy/mlogger-fd/internal/handler"`; no new import needed in main.go.
  </action>
  <verify>
    <automated>go test ./internal/handler/ -run TestBonuses -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'func GetBonuses' internal/handler/bonus.go` returns 1
    - `grep -c 'func PutBonuses' internal/handler/bonus.go` returns 1
    - `grep -c 'model.DefaultBonuses' internal/handler/bonus.go` returns at least 1
    - `grep -c 'INSERT OR REPLACE INTO bonus_claims' internal/handler/bonus.go` returns at least 1
    - `grep -c 'r.Get("/bonuses"' main.go` returns 1
    - `grep -c 'r.Put("/bonuses"' main.go` returns 1
    - `grep -c 'GetBonuses(database' main.go` returns 1
    - `grep -c 'PutBonuses(database' main.go` returns 1
    - `curl -s http://localhost:8080/api/bonuses` returns JSON object with 18 keys (after server start)
    - `go test ./internal/handler/ -run TestBonuses -count=1` exits 0
  </acceptance_criteria>
  <done>handler/bonus.go with GetBonuses/PutBonuses exists. main.go has /api/bonuses GET and PUT routes wired. All handler tests pass. GET returns full bonus list, PUT persists claims with validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API (PUT /api/bonuses) | Untrusted JSON body with bonus_id keys, claimed booleans, and count integers from browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | PutBonuses handler — unknown bonus_id injection | mitigate | Validate each bonus_id key against `model.DefaultBonuses` list; unknown keys silently skipped, not inserted — per RESEARCH.md known threat: "Unknown bonus_id injection → validate against known list in PUT handler" |
| T-04-02 | Tampering | PutBonuses handler — count integer overflow / negative values | mitigate | `model.ValidateBonusClaim` validates count is non-negative and ≤ MaxCount per bonus type; returns 400 with error message — per ASVS V5: "Bonus claim counts must be validated (non-negative integers, max bounds per bonus type)" |
| T-04-03 | Tampering | PutBonuses handler — malformed JSON body | mitigate | `json.NewDecoder(r.Body).Decode` error returns 400 with `{"error": "invalid JSON"}` — follows existing config.go pattern |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced in this phase; all deps already audited in prior phases |
</threat_model>

<verification>
After both tasks complete:
1. Start server: `go run .` (port 8080)
2. `curl -s http://localhost:8080/api/bonuses | python3 -m json.tool | head -20` — returns 18 bonus items with claimed=false, count=0
3. `curl -s -X PUT http://localhost:8080/api/bonuses -H 'Content-Type: application/json' -d '{"emergency_power":{"claimed":true,"count":3},"media_publicity":{"claimed":true,"count":0}}'` — returns 200 with full state including the two claims
4. `curl -s http://localhost:8080/api/bonuses` — confirms emergency_power.claimed=true, emergency_power.count=3
</verification>

<success_criteria>
- `bonus_claims` table exists in SQLite schema, created idempotently via `CREATE TABLE IF NOT EXISTS`
- `internal/model/bonus.go` has 18-item ARRL 2026 bonus list per D-01, validation per D-02, and points calculation
- `GET /api/bonuses` returns full bonus map (all 18 items) even when no claims exist yet
- `PUT /api/bonuses` persists claims, validates counts per bonus type limits, rejects unknown bonus_ids silently
- Score formula `CalculateBonusPoints` follows ARRL rules (post-multiplier addition)
</success_criteria>

<output>
Create `.planning/phases/04-field-day-features-testing/04-01-SUMMARY.md` when done
</output>
