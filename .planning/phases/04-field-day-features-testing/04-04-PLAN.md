---
phase: 04-field-day-features-testing
plan: 04
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - internal/handler/backup.go
  - main.go
  - frontend/src/lib/api.js
  - frontend/src/routes/+page.svelte
  - internal/handler/simtest/simtest_test.go
autonomous: false
requirements: [BKUP-01]

must_haves:
  truths:
    - "GET /api/backup/db streams the live SQLite file with Content-Disposition: attachment and timestamped filename per D-10"
    - "Backup download uses io.Copy streaming — no VACUUM INTO, no exclusive locks per D-11"
    - "Backup button (↓ Backup) appears in header-right next to Export Cabrillo per D-09"
    - "Backup button triggers download and shows 'Backup downloaded' toast for 2 seconds following StationConfig save-feedback pattern"
    - "`go test ./internal/handler/simtest/ -run TestSimulation -timeout 120s` exits 0, validating 3 clients × ~70 QSOs with data integrity checks per D-13"
    - "Simulation test covers: no QSOs lost, dupe detection correct, stats accurate, WebSocket broadcast consistency"
    - "Field test checklist exists as FIELD_TEST_CHECKLIST.md with minimal verification steps for outdoor setup per D-14"
  artifacts:
    - path: "internal/handler/backup.go"
      provides: "DownloadBackup HTTP handler streaming SQLite file"
      exports: ["DownloadBackup"]
    - path: "main.go"
      provides: "Chi router wiring for GET /api/backup/db"
      contains: "r.Get(\"/backup/db\""
    - path: "frontend/src/lib/api.js"
      provides: "downloadBackup() function for one-click download"
      contains: "downloadBackup"
    - path: "frontend/src/routes/+page.svelte"
      provides: "Backup button in header + 'Backup downloaded' toast"
      contains: "downloadBackup"
    - path: "internal/handler/simtest/simtest_test.go"
      provides: "Multi-client integration test validating data integrity under simulated load"
      contains: "TestSimulation"
  key_links:
    - from: "+page.svelte"
      to: "/api/backup/db"
      via: "window.location.href assignment in downloadBackup()"
      pattern: "window.location.href.*backup"
    - from: "handler/backup.go"
      to: "SQLite .db file"
      via: "os.Open(dbPath) + io.Copy(w, f)"
      pattern: "io.Copy"
    - from: "simtest_test.go"
      to: "handler.CreateQSO, handler.GetStats, handler.ServeWS"
      via: "chi router with all production handlers wired"
      pattern: "handler.CreateQSO"
---

<objective>
Add one-click database backup download and a Go integration test simulating multi-client Field Day logging. After this plan, the operator can download a timestamped SQLite backup from the header bar, and `go test ./...` includes a simulation test validating data integrity under 3-client concurrent load with 200+ QSOs.

Purpose: BKUP-01 (one-click backup) per D-09/D-10/D-11 protects against data loss during Field Day. The simulation test per D-12/D-13 provides automated confidence that the system handles real Field Day load before deployment. A minimal field test checklist per D-14 guides the final outdoor verification.
Output: `handler/backup.go`, `main.go` backup route, `api.js` downloadBackup, `+page.svelte` backup button + toast, `simtest_test.go` integration test, `FIELD_TEST_CHECKLIST.md`.
</objective>

<execution_context>
@/home/jeremy/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/jeremy/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-field-day-features-testing/04-CONTEXT.md
@.planning/phases/04-field-day-features-testing/04-RESEARCH.md § "SQLite WAL Backup Pattern" (lines 601-673) and § "Go Integration Test Pattern" (lines 676-744)
@.planning/phases/04-field-day-features-testing/04-PATTERNS.md § "Pattern 4: handler/backup.go" (lines 265-345) and § "Pattern 13: simtest" (lines 958-1112)

# Key pattern sources
@internal/handler/export.go (file download pattern — Content-Disposition, response headers)
@internal/handler/qso_test.go (test DB setup pattern with in-memory SQLite, t.Cleanup)
@internal/handler/ws_test.go (httptest.Server + gorilla/websocket + goroutine concurrent QSO pattern)
@main.go (Chi router closure pattern, dbPath env var at line 23)
@frontend/src/lib/api.js (existing functions structure)
@frontend/src/routes/+page.svelte (header-right layout, exportCabrillo function pattern at line 40, backupToast state from Plan 04-02)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create backup handler, route, API function, and frontend button</name>
  <files>internal/handler/backup.go, main.go, frontend/src/lib/api.js, frontend/src/routes/+page.svelte</files>
  <read_first>
    - internal/handler/export.go (ExportCabrillo file download pattern at lines 12-29 — Content-Disposition header, error handling with http.Error)
    - main.go (dbPath variable at line 23-26 from FDLOGGER_DB_PATH env; Chi route closure pattern at lines 43-74)
    - frontend/src/lib/api.js (existing function patterns; downloadBackup follows exportCabrillo approach — window.location.href)
    - frontend/src/routes/+page.svelte (exportCabrillo function at line 40-42 — copy pattern; header-right template at lines 61-64; backupToast state added in Plan 04-02)
    - .planning/phases/04-field-day-features-testing/04-RESEARCH.md § "Backup Implementation" (lines 615-651 — full handler code pattern)
    - .planning/phases/04-field-day-features-testing/04-RESEARCH.md § "Backup Button Frontend" (lines 661-673 — downloadBackup function, button placement)
    - .planning/phases/04-field-day-features-testing/04-PATTERNS.md § "Pattern 4: handler/backup.go" (lines 265-345) and § "Pattern 7: main.go" (lines 478-538) and § "Pattern 8: api.js" (lines 541-604) and § "Pattern 12: +page.svelte" (lines 874-955)
  </read_first>
  <behavior>
    - Test 1: GET /api/backup/db returns 200 with Content-Type application/octet-stream and Content-Disposition header containing timestamped filename
    - Test 2: Backup response body matches the SQLite file on disk (verify first 16 bytes are SQLite format 3 magic header)
    - Test 3: Backup download works while QSOs are being created concurrently (WAL mode concurrency — no blocking)
    - Test 4: Backup endpoint returns 500 with generic error "Failed to open database" when dbPath is invalid (no filesystem path exposed per ASVS V7)
    - Test 5: downloadBackup() function in api.js triggers navigation to /api/backup/db
    - Test 6: Backup button in header-right calls handleBackup() which triggers download and shows 2-second toast
  </behavior>
  <action>
    per D-09, D-10, D-11: **Create `internal/handler/backup.go`** — Database backup download handler:

    1. Package `handler`. Imports: `"database/sql"`, `"fmt"`, `"io"`, `"net/http"`, `"os"`, `"time"`.
    2. Function signature: `func DownloadBackup(db *sql.DB, dbPath string, w http.ResponseWriter, r *http.Request)` — takes dbPath as additional parameter (from main.go closure).
    3. Implementation per RESEARCH.md lines 628-651:
       - Optionally flush WAL: `db.Exec("PRAGMA wal_checkpoint(TRUNCATE)")` — ignore error (best-effort).
       - Generate timestamped filename per D-10: `now := time.Now().UTC().Format("20060102_150405")` → `filename := fmt.Sprintf("fdlogger_backup_%s.db", now)`.
       - Open db file: `f, err := os.Open(dbPath)`. On error: `http.Error(w, "Failed to open database", http.StatusInternalServerError)` — generic message per ASVS V7, no filesystem path exposed.
       - `defer f.Close()`.
       - Set response headers: `Content-Type: application/octet-stream`, `Content-Disposition: attachment; filename="<filename>"`.
       - Stream: `io.Copy(w, f)`. If io.Copy fails (client disconnect), silently return — not a server error.
    4. Follow ExportCabrillo pattern from handler/export.go: set headers before writing, use `fmt.Sprintf` for Content-Disposition.

    per D-11: The `PRAGMA wal_checkpoint(TRUNCATE)` is optional but recommended — it flushes committed WAL pages to the main .db file, ensuring the backup includes all data committed up to that point. WAL mode guarantees the .db file is always consistent even without checkpoint.

    per D-09: **Modify `main.go`** — Add backup route. In the `r.Route("/api", ...)` block, add after the `/bonuses` routes and before `/sync`:
    ```go
    r.Get("/backup/db", func(w http.ResponseWriter, r *http.Request) {
        handler.DownloadBackup(database, dbPath, w, r)
    })
    ```
    The `dbPath` variable already exists at line 23 (`dbPath := os.Getenv("FDLOGGER_DB_PATH")` / default `"fdlogger.db"`). The closure captures both `database` and `dbPath`.

    per D-09: **Modify `frontend/src/lib/api.js`** — Add downloadBackup function after the getBonuses/putBonuses functions:
    ```javascript
    export function downloadBackup() {
        window.location.href = `${BASE_URL}/api/backup/db`;
    }
    ```
    This follows the exact same pattern as `exportCabrillo()` in +page.svelte line 40-42 (window.location.href triggers browser download via navigation). No fetch needed — the Content-Disposition header triggers a file save dialog.

    per D-09: **Modify `frontend/src/routes/+page.svelte`** — Add backup button + toast:

    1. Import `downloadBackup` from api.js: `import { downloadBackup } from '$lib/api.js';` (add to existing imports).

    2. Add `handleBackup()` function in script:
    ```javascript
    function handleBackup() {
        downloadBackup();
        backupToast = true;
        if (backupTimer) clearTimeout(backupTimer);
        backupTimer = setTimeout(() => { backupToast = false; }, 2000);
    }
    ```
    The `backupToast` and `backupTimer` state variables were already created in Plan 04-02 Task 1. This function connects the button to the existing toast infrastructure.

    3. In header-right template, add backup button after Export Cabrillo button (after line 63):
    ```svelte
    <button class="export-btn" onclick={handleBackup}>↓ Backup</button>
    ```
    Reuses `.export-btn` CSS class (same styling as Export Cabrillo — transparent bg, 2px solid border, white text).

    4. The backup toast template was already placed in Plan 04-02. Verify it renders when `backupToast` is true (a `<span class="saved-msg">Backup downloaded</span>` near header-right). If the toast wasn't placed, add it now:
    ```svelte
    {#if backupToast}
        <span class="saved-msg" style="font-size:12px;color:var(--color-success);font-weight:600;">Backup downloaded</span>
    {/if}
    ```
    Place after the backup button in header-right.
  </action>
  <verify>
    <automated>go test ./internal/handler/ -run TestBackup -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/handler/backup.go` file exists
    - `grep -c 'func DownloadBackup' internal/handler/backup.go` returns 1
    - `grep -c 'io.Copy' internal/handler/backup.go` returns at least 1
    - `grep -c 'fdlogger_backup_' internal/handler/backup.go` returns at least 1
    - `grep -c 'wal_checkpoint' internal/handler/backup.go` returns at least 1
    - `grep -c 'application/octet-stream' internal/handler/backup.go` returns 1
    - `grep -c '"Failed to open database"' internal/handler/backup.go` returns 1 (generic error, no path exposed)
    - No filesystem path appears in error messages returned to client (only "Failed to open database")
    - `grep -c 'r.Get("/backup/db"' main.go` returns 1
    - `grep -c 'DownloadBackup(database' main.go` returns 1
    - `grep -c 'downloadBackup' frontend/src/lib/api.js` returns at least 1
    - `grep -c 'window.location.href.*backup' frontend/src/lib/api.js` returns at least 1
    - `grep -c 'handleBackup' frontend/src/routes/+page.svelte` returns at least 1
    - Backup button (↓ Backup) appears after Export Cabrillo in header-right
    - `grep -c 'Backup downloaded' frontend/src/routes/+page.svelte` returns at least 1
    - `go test ./internal/handler/ -run TestBackup -count=1` exits 0
  </acceptance_criteria>
  <done>handler/backup.go streams SQLite file with timestamped filename. main.go has /api/backup/db route. api.js has downloadBackup(). +page.svelte has ↓ Backup button and 2-second toast. Handler tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create multi-client simulation integration test</name>
  <files>internal/handler/simtest/simtest_test.go</files>
  <read_first>
    - internal/handler/qso_test.go (test DB setup with in-memory SQLite + t.Cleanup at lines 14-39)
    - internal/handler/ws_test.go (TestCreateQSOBroadcast with httptest.Server + gorilla/websocket at lines 44-60; TestCreateQSOConcurrent with goroutines + errChan at lines 144-173)
    - main.go (full Chi router setup at lines 38-81 — the simtest must replicate all routes)
    - internal/handler/stats.go (GetStats signature and response shape)
    - internal/handler/qso.go (CreateQSO signature, ListQSOs signature)
    - internal/ws/hub.go (Hub struct, NewHub, Run)
    - .planning/phases/04-field-day-features-testing/04-RESEARCH.md § "Simulation Test Design" (lines 687-722 — 3 clients, ~70 QSOs each, integrity assertions)
    - .planning/phases/04-field-day-features-testing/04-PATTERNS.md § "Pattern 13: simtest" (lines 958-1112)
  </read_first>
  <behavior>
    - Test 1: TestSimulation sets up in-memory SQLite with full schema (qsos + station_config + bonus_claims), creates httptest.Server with chi router, spawns 3 goroutine clients
    - Test 2: Each simulated client connects WebSocket, submits ~70 mixed QSOs (unique + deliberate duplicates), tracks received WebSocket broadcasts
    - Test 3: After all clients finish, verifies total non-dupe QSO count from GET /api/stats matches expected
    - Test 4: Verifies every submitted QSO appears in GET /api/qso response
    - Test 5: Verifies known duplicate QSOs have is_dupe=true and points=0
    - Test 6: Verifies score = (rawPoints * multiplier) + bonusPoints is accurate
    - Test 7: Verifies all WebSocket clients received total broadcast count matches total submitted (deduplication applied)
  </behavior>
  <action>
    per D-12, D-13: **Create `internal/handler/simtest/simtest_test.go`** — Multi-client Field Day simulation test:

    Package: `package simtest` (separate package to avoid circular imports with `handler` package — test code references handler functions via `handler.CreateQSO` etc).

    1. **setupSimTestDB(t *testing.T) *sql.DB** — Creates in-memory SQLite with `file::memory:?_pragma=journal_mode(WAL)&_pragma=busy_timeout(5000)&cache=shared` and `db.SetMaxOpenConns(1)`. Creates all tables: `qsos`, `station_config`, `bonus_claims` using the same CREATE TABLE statements from schema.sql. Inserts default station_config row (id=1 with default values). Registers `t.Cleanup(func() { db.Close() })`. Returns db. Follow exactly the pattern from handler/qso_test.go lines 14-39.

    2. **setupSimRouter(db *sql.DB, hub *ws.Hub) chi.Router** — Builds a chi.Router with all production routes wired, replicating main.go lines 43-74. Each route closure calls the real handler function. Routes: GET /api/health, GET /api/check-dupe (handler.CheckDupeHandler), GET /api/stats (handler.GetStats), POST /api/qso (handler.CreateQSO with hub), GET /api/qso (handler.ListQSOs), PUT /api/qso/{id} (handler.UpdateQSO), GET /api/bonuses (handler.GetBonuses), PUT /api/bonuses (handler.PutBonuses), POST /api/sync (handler.SyncQSOs with hub). Also: GET /ws (handler.ServeWS with hub).

    3. **TestSimulation(t *testing.T)** — Main test function:
       - Setup: Create hub, `go hub.Run()`, `time.Sleep(10 * time.Millisecond)`. Create test DB via `setupSimTestDB(t)`. Create router via `setupSimRouter(db, hub)`. Start `httptest.NewServer(router)`, `defer srv.Close()`.
       - Client configuration: 3 clients, ~70 QSOs each (~210 total). Mix of unique callsigns and deliberate duplicates.
       - Simulated client goroutine: Each client:
         a. Connects WebSocket to `wsURL` (converted from http to ws scheme).
         b. Starts a goroutine to read WebSocket messages, incrementing `broadcastCount` per `qso_created` message (deduplicating by ID).
         c. Posts QSOs via `http.Post(srv.URL+"/api/qso", ...)` with JSON body — mix of unique callsigns (e.g., K1AAA, K2BBB, ...) and deliberate duplicates (same callsign+band+mode repeated).
         d. Waits for all posts to complete.
       - Coordination: Use `sync.WaitGroup` to wait for all 3 clients to finish submitting.
       - Broadcast tracking: After all submissions, wait 500ms for WebSocket broadcasts to arrive, then count total received broadcasts.
       - **Integrity assertions (per D-13):**
         a. GET /api/stats → verify `total` equals the expected non-dupe QSO count.
         b. GET /api/qso?limit=9999 → verify response array length equals total submitted QSOs.
         c. Scan GET /api/qso response for known dupe callsigns → verify `is_dupe=true` and `points=0`.
         d. GET /api/stats → verify `score == (rawPoints * multiplier) + bonusPoints` (ARRL formula).
         e. All 3 WebSocket clients' `broadcastCount` values are close to total submitted (within a few — some race conditions possible on last messages).
       - Use only `testing` stdlib assertions (no testify). Use `t.Errorf` for failures.
       - Target runtime: < 60 seconds wall-clock (not real 2 hours — the 200+ QSO count validates load, not wall time).

    4. The test must be runnable via: `go test ./internal/handler/simtest/ -run TestSimulation -timeout 120s`
  </action>
  <verify>
    <automated>go test ./internal/handler/simtest/ -run TestSimulation -timeout 120s -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/handler/simtest/simtest_test.go` file exists
    - `grep -c 'func TestSimulation' internal/handler/simtest/simtest_test.go` returns 1
    - `grep -c 'setupSimTestDB' internal/handler/simtest/simtest_test.go` returns at least 1
    - `grep -c 'setupSimRouter' internal/handler/simtest/simtest_test.go` returns at least 1
    - `grep -c 'httptest.NewServer' internal/handler/simtest/simtest_test.go` returns at least 1
    - `grep -c 'gorilla/websocket' internal/handler/simtest/simtest_test.go` returns at least 1
    - `grep -c 'sync.WaitGroup' internal/handler/simtest/simtest_test.go` returns at least 1
    - `grep -c 'handler.CreateQSO' internal/handler/simtest/simtest_test.go` returns at least 1
    - `grep -c 'handler.GetStats' internal/handler/simtest/simtest_test.go` returns at least 1
    - Simulation creates 3 goroutine clients submitting ~70 QSOs each (200+ total)
    - Integrity checks: QSO count, dupe marking, score calculation, broadcast receipt
    - `go test ./internal/handler/simtest/ -run TestSimulation -timeout 120s -count=1` exits 0
  </acceptance_criteria>
  <done>simtest_test.go passes: 3 simulated clients, 200+ QSOs, all integrity assertions passing (no lost QSOs, correct dupe detection, accurate stats, broadcast consistency).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Create field test checklist (manual — D-14)</name>
  <files>FIELD_TEST_CHECKLIST.md</files>
  <read_first>
    - .planning/ROADMAP.md § Phase 4 success criteria (line 145: "Tested in a real outdoor setup (park, tent) with at least 2 operators")
    - .planning/phases/04-field-day-features-testing/04-CONTEXT.md § D-14 (line 32: "Field test is a minimal checklist — set up in a park, log QSOs from 2+ devices, verify everything works. No formal test documentation required.")
  </read_first>
  <action>per D-14: Create `FIELD_TEST_CHECKLIST.md` in project root with a minimal checklist covering all Phase 4 success criteria: (1) Set up server (RPi or laptop) with WiFi AP or LAN switch, (2) Connect 2+ devices to the server, (3) Verify each operator can log QSOs, (4) Verify real-time QSO display across devices, (5) Test offline logging (disable WiFi on one device, log, reconnect, verify sync), (6) Test backup download, (7) Test Cabrillo export, (8) Test bonus tracker toggle + score reflection, (9) Verify dark mode usability outdoors in sunlight, (10) Test audio alerts with mute toggle. Keep checklist minimal — 10-15 items, no pass/fail columns, no sign-off blocks. No formal test documentation per D-14.</action>
  <verify>
    <automated>grep -c 'Set up server' FIELD_TEST_CHECKLIST.md</automated>
  </verify>
  <what-built>A minimal field test checklist for outdoor verification with 2+ operators, per D-14.</what-built>
  <how-to-verify>
    1. Read `FIELD_TEST_CHECKLIST.md` in project root
    2. Confirm checklist is minimal (10-15 items)
    3. Confirm it covers all Phase 4 success criteria: bonus tracker, audio, backup, multi-client, offline sync
    4. Confirm no formal test documentation (no pass/fail columns, no sign-off blocks)
  </how-to-verify>
  <resume-signal>Type "approved" or describe changes needed</resume-signal>
  <done>FIELD_TEST_CHECKLIST.md exists with minimal outdoor test steps covering all Phase 4 features. User approves checklist content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API (GET /api/backup/db) | Browser triggers file download; dbPath is server-side env var, not user input |
| Simulation test boundaries | All in-process — no network boundaries crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Information Disclosure | handler/backup.go — backup error messages | mitigate | ASVS V7: error messages use generic "Failed to open database" — no filesystem path exposed. dbPath is server-side env var, not from request |
| T-04-12 | Tampering | handler/backup.go — path traversal | accept | dbPath is from FDLOGGER_DB_PATH env var in main.go, not from user input — no traversal risk per RESEARCH.md known threat pattern |
| T-04-13 | Denial of Service | handler/backup.go — large file streaming | accept | SQLite DB for Field Day is small (< 10MB for 2000 QSOs). io.Copy uses OS-level sendfile on Linux — efficient. Trusted LAN environment |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced in this phase |
</threat_model>

<verification>
After both tasks complete:
1. Start server, click ↓ Backup in header → browser downloads `fdlogger_backup_YYYYMMDD_HHMMSS.db`
2. Verify "Backup downloaded" toast appears for 2 seconds
3. Verify downloaded .db file can be opened with `sqlite3 fdlogger_backup_*.db ".tables"` showing qsos, station_config, bonus_claims
4. Run simulation test: `go test ./internal/handler/simtest/ -run TestSimulation -timeout 120s -v` → passes with all integrity checks
5. Review FIELD_TEST_CHECKLIST.md and approve
</verification>

<success_criteria>
- One-click backup downloads timestamped SQLite file without blocking active logging (WAL mode)
- Backup button and toast follow existing StationConfig/Export patterns
- Simulation test passes: 3 clients × 200+ QSOs, all 7 integrity assertions green
- Field test checklist ready for outdoor deployment verification
</success_criteria>

<output>
Create `.planning/phases/04-field-day-features-testing/04-04-SUMMARY.md` when done
</output>
