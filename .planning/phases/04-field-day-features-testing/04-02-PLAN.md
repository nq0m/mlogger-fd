---
phase: 04-field-day-features-testing
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - frontend/src/lib/api.js
  - frontend/src/lib/stores/qso.svelte.js
  - frontend/src/lib/components/BonusTracker.svelte
  - frontend/src/routes/+page.svelte
  - internal/handler/stats.go
  - internal/cabrillo/cabrillo.go
  - frontend/src/lib/components/StatsBar.svelte
autonomous: true
requirements: [BON-01, BON-02]

must_haves:
  truths:
    - "BonusTracker component renders as an expandable header panel with a ★ Bonuses toggle button, positioned between StationConfig and Export Cabrillo"
    - "Bonus list shows all 18 bonus items with toggle checkboxes for boolean bonuses and count number inputs for counted bonuses"
    - "Saving bonus claims persists to server via PUT /api/bonuses and shows a 2-second 'Saved!' toast per D-09 pattern"
    - "Bonus claims survive page reload via localStorage backup (fdlogger_bonus_claims key)"
    - "Live score display in StatsBar shows a new 'Bonus' stat block with gold/amber color"
    - "GET /api/stats response includes bonus_points field, and score is (rawPoints * multiplier) + bonusPoints per ARRL rules"
    - "Cabrillo export includes CLAIMED-SCORE with bonus points added, SOAPBOX lines for each claimed bonus, and SOAPBOX total bonus points"
  artifacts:
    - path: "frontend/src/lib/components/BonusTracker.svelte"
      provides: "Expandable bonus claim panel with toggle + count UI"
      min_lines: 60
    - path: "frontend/src/lib/api.js"
      provides: "getBonuses() and putBonuses() API client functions"
      contains: "getBonuses"
    - path: "frontend/src/lib/stores/qso.svelte.js"
      provides: "bonusClaims reactive state, bonus_points in stats default"
      contains: "bonusClaims"
    - path: "frontend/src/routes/+page.svelte"
      provides: "Header bar integration: BonusTracker component + backup toast state"
      contains: "BonusTracker"
    - path: "internal/handler/stats.go"
      provides: "Stats endpoint with bonus_points in response and ARRL-correct score"
      contains: "bonus_points"
    - path: "internal/cabrillo/cabrillo.go"
      provides: "Cabrillo export with CLAIMED-SCORE including bonus + SOAPBOX bonus lines"
      contains: "SOAPBOX: Bonus:"
    - path: "frontend/src/lib/components/StatsBar.svelte"
      provides: "Bonus stat block display between Pts and Mult"
      contains: "bonus"
  key_links:
    - from: "BonusTracker.svelte"
      to: "/api/bonuses"
      via: "api.js getBonuses()/putBonuses()"
      pattern: "getBonuses\\(\\)"
    - from: "stats.go"
      to: "bonus_claims table"
      via: "COALESCE(SUM(CASE...)) query"
      pattern: "bonus_claims"
    - from: "cabrillo.go"
      to: "bonus_claims table"
      via: "QueryRow for bonus points + Query for SOAPBOX rows"
      pattern: "bonus_claims"
---

<objective>
Deliver the Bonus Tracker frontend UI and integrate bonus points into the live scoring display and Cabrillo export. After this plan, an operator can open the ★ Bonuses panel from the header, toggle boolean bonuses, enter counts for counted bonuses, save claims that persist server-side and in localStorage, and see bonus points reflected in both the live score display and the exported Cabrillo file.

Purpose: Complete the full-stack bonus tracking feature per D-03 (expandable header panel), D-04 (server persistence + localStorage backup), and D-05 (score and Cabrillo integration). This is the second half of the bonus tracker vertical slice.
Output: BonusTracker.svelte component, api.js extended, qso.svelte.js bonusClaims state, +page.svelte header integration, stats.go and cabrillo.go modified for bonus scoring, StatsBar.svelte bonus display.
</objective>

<execution_context>
@/home/jeremy/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/jeremy/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-field-day-features-testing/04-CONTEXT.md
@.planning/phases/04-field-day-features-testing/04-RESEARCH.md
@.planning/phases/04-field-day-features-testing/04-PATTERNS.md

# Key pattern sources
@frontend/src/lib/api.js
@frontend/src/lib/stores/qso.svelte.js
@frontend/src/routes/+page.svelte
@frontend/src/lib/components/StationConfig.svelte
@frontend/src/lib/components/StatsBar.svelte
@internal/handler/stats.go
@internal/cabrillo/cabrillo.go
@internal/model/bonus.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build BonusTracker frontend — API client, store, component, header</name>
  <files>frontend/src/lib/api.js, frontend/src/lib/stores/qso.svelte.js, frontend/src/lib/components/BonusTracker.svelte, frontend/src/routes/+page.svelte</files>
  <read_first>
    - frontend/src/lib/api.js (existing getStationConfig/putStationConfig pattern at lines 66-85 — COPY the exact structure for getBonuses/putBonuses)
    - frontend/src/lib/stores/qso.svelte.js (stats $state default at lines 3-11 — add bonus_points: 0; ws.svelte.js line 7 for object-based $state export pattern)
    - frontend/src/lib/components/StationConfig.svelte (entire file — expandable panel toggle, saved toast, absolute positioning, form submit, onMount load — this is the EXACT template for BonusTracker)
    - frontend/src/routes/+page.svelte (script imports at lines 1-11, header-right template at lines 61-64, export-btn CSS at lines 128-142)
    - .planning/phases/04-field-day-features-testing/04-PATTERNS.md § "Pattern 8: api.js" (lines 541-604), § "Pattern 9: qso.svelte.js" (lines 607-647), § "Pattern 10: BonusTracker.svelte" (lines 650-787), § "Pattern 12: +page.svelte" (lines 874-955)
    - internal/model/bonus.go (DefaultBonuses list — used to build the bonus display items fetched from GET /api/bonuses response)
  </read_first>
  <action>
    **Part A: api.js** — per D-04. Append `getBonuses()` and `putBonuses(data)` functions to `frontend/src/lib/api.js` after `putStationConfig` (after line 85). Copy the exact pattern from getStationConfig/putStationConfig:
    - `getBonuses()`: `fetch(\`\${BASE_URL}/api/bonuses\`)`, check `res.ok`, return `res.json()`, throw on failure.
    - `putBonuses(data)`: `fetch(\`\${BASE_URL}/api/bonuses\`, { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) })`, parse JSON response, throw on !res.ok with json.error.
    
    **Part B: qso.svelte.js** — per D-04 (localStorage hybrid pattern). Add two things:
    1. Add `bonus_points: 0` to the `stats` $state default object (after line 9, before the closing `}`).
    2. Add `export const bonusClaims = $state({})` — object-based $state export (Svelte 5 pattern per wsState/queueState precedent). This holds the map `{ bonus_id: { claimed: bool, count: int } }`.
    
    **Part C: BonusTracker.svelte** — per D-03, D-04. Create new component at `frontend/src/lib/components/BonusTracker.svelte` following the StationConfig.svelte pattern exactly:
    - **Imports:** `onMount` from svelte, `getBonuses`/`putBonuses` from $lib/api.js, `bonusClaims` from $lib/stores/qso.svelte.js.
    - **State:** `expanded` ($state false), `saved` ($state false), `saveTimer`.
    - **onMount:** Try localStorage first (`localStorage.getItem('fdlogger_bonus_claims')` → parse JSON, merge into bonusClaims object). Then fetch from server via `getBonuses()` and merge into bonusClaims (server is source of truth per D-04). On first load when both are empty, initialize all 18 bonus items with claimed=false, count=0.
    - **toggle():** flips expanded.
    - **handleSubmit(e):** Calls `putBonuses(bonusClaims)`. On success: set saved=true, save to localStorage (`localStorage.setItem('fdlogger_bonus_claims', JSON.stringify(bonusClaims))`), clear timer, setTimeout 2s to clear saved.
    - **Template:** `<button class="config-toggle" onclick={toggle}>` with `★ Bonuses` label (star icon per RESEARCH.md diagram). Panel div with `class="config-panel"` (reuses StationConfig's absolute positioning CSS: position absolute, top 44px, right 8px, z-index 100, min-width 260px). Inside: iterate over `Object.entries(bonusClaims)` — for each bonus, render a checkbox input (bind:checked to entry.claimed) and if the bonus is a counted type (check against a hardcoded set: emergency_power, message_handling, youth_participation, gota_bonus), also render a number input (bind:value to entry.count). Use `{#each Object.keys(bonusClaims) as bonusId}` and display the bonus name (derive from DefaultBonuses via a name map or import from model if possible; otherwise use a hardcoded name lookup object in the component). Save button + saved toast.
    - **CSS:** Reuse StationConfig's `.config-toggle` and `.config-panel` classes from the global CSS or define inline matching the exact same rules (transparent bg, 2px solid --color-surface border, etc.). The existing styles in StationConfig.svelte are scoped — BonusTracker must define its own matching styles or share the class names if styles are in app.css. Define styles inline following the exact same values from StationConfig.svelte lines 127-158.
    
    **Part D: +page.svelte header** — per D-03, D-09 (backup toast prep). 
    1. Add import: `import BonusTracker from '$lib/components/BonusTracker.svelte';`
    2. In header-right (line 62), insert `<BonusTracker />` between `<StationConfig />` and the Export button.
    3. Also prep the backup toast state for Plan 04-04: add `let backupToast = $state(false); let backupTimer;` in script, and add a `{#if backupToast}<span class="saved-msg" style="...">Backup downloaded</span>{/if}` near the header-right. The actual `downloadBackup` function import and button will be added in Plan 04-04 — just create the state and toast template now to avoid merge conflicts.
  </action>
  <verify>
    <automated>vitest run src/lib/components/BonusTracker.test.js 2>/dev/null || echo "MISSING — Wave 0 must create frontend/src/lib/components/BonusTracker.test.js first"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'getBonuses' frontend/src/lib/api.js` returns at least 1
    - `grep -c 'putBonuses' frontend/src/lib/api.js` returns at least 1
    - `grep -c 'bonus_points' frontend/src/lib/stores/qso.svelte.js` returns at least 1
    - `grep -c 'bonusClaims' frontend/src/lib/stores/qso.svelte.js` returns at least 1
    - `frontend/src/lib/components/BonusTracker.svelte` file exists
    - `grep -c 'toggle\|expanded' frontend/src/lib/components/BonusTracker.svelte` returns at least 2
    - `grep -c 'getBonuses' frontend/src/lib/components/BonusTracker.svelte` returns at least 1
    - `grep -c 'putBonuses' frontend/src/lib/components/BonusTracker.svelte` returns at least 1
    - `grep -c 'localStorage.getItem.*fdlogger_bonus_claims' frontend/src/lib/components/BonusTracker.svelte` returns at least 1
    - `grep -c 'localStorage.setItem.*fdlogger_bonus_claims' frontend/src/lib/components/BonusTracker.svelte` returns at least 1
    - `grep -c 'BonusTracker' frontend/src/routes/+page.svelte` returns at least 1
    - `grep -c 'import BonusTracker' frontend/src/routes/+page.svelte` returns 1
    - BonusTracker is placed between StationConfig and Export in header-right (verify: line order in template has StationConfig, then BonusTracker, then Export)
    - `grep -c 'backupToast' frontend/src/routes/+page.svelte` returns at least 2 (state declaration + template usage)
  </acceptance_criteria>
  <done>BonusTracker.svelte renders as expandable panel with toggle checkboxes + count inputs. api.js has getBonuses/putBonuses. qso.svelte.js has bonusClaims state and bonus_points default. +page.svelte has BonusTracker in header and backup toast prep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate bonus points into scoring — stats.go, cabrillo.go, StatsBar.svelte</name>
  <files>internal/handler/stats.go, internal/cabrillo/cabrillo.go, frontend/src/lib/components/StatsBar.svelte</files>
  <read_first>
    - internal/handler/stats.go (current score calculation at line 45: `score := rawPoints * multiplier`; response map at lines 66-74)
    - internal/cabrillo/cabrillo.go (current score at line 78: `score := rawPoints * multiplier`; CLAIMED-SCORE output at line 79; QSO loop at lines 81-123)
    - frontend/src/lib/components/StatsBar.svelte (stat blocks at lines 20-45 — Pts, Mult, Score; CSS at lines 86-117)
    - internal/model/bonus.go (CalculateBonusPoints function from Plan 04-01)
    - .planning/phases/04-field-day-features-testing/04-RESEARCH.md § "Score Calculation Integration" (lines 463-488 — bonus points query SQL, ARRL formula: score = (rawPoints * multiplier) + bonusPoints)
    - .planning/phases/04-field-day-features-testing/04-RESEARCH.md § "Cabrillo Export Integration" (lines 489-506 — SOAPBOX and X-BONUS line format)
    - .planning/phases/04-field-day-features-testing/04-PATTERNS.md § "Pattern 5: stats.go" (lines 348-413) and § "Pattern 6: cabrillo.go" (lines 416-476) and § "Pattern 14: StatsBar.svelte" (lines 1115-1200)
  </read_first>
  <behavior>
    - Test 1: GET /api/stats returns bonus_points field in JSON response (number, 0 when no bonuses claimed)
    - Test 2: GET /api/stats returns correct bonus_points when bonus_claims has claimed items (e.g., emergency_power claimed + count 3 = 300 bonus_points)
    - Test 3: GET /api/stats score = (rawPoints * multiplier) + bonusPoints (post-multiplier per ARRL rules)
    - Test 4: Cabrillo export CLAIMED-SCORE includes bonus points: (rawPoints * multiplier) + bonusPoints
    - Test 5: Cabrillo export includes SOAPBOX lines for claimed bonuses and SOAPBOX total bonus points line
    - Test 6: StatsBar displays Bonus stat block between Pts and Mult with gold/amber color
  </behavior>
  <action>
    per D-05: **Modify `internal/handler/stats.go`** — Add bonus points query after multiplier calculation (after line 43), before score calculation (line 45):

    1. Add a bonus points query:
    ```go
    var bonusPoints int
    db.QueryRow(`SELECT COALESCE(SUM(CASE
        WHEN bonus_id = 'emergency_power' THEN claimed * count * 100
        WHEN bonus_id = 'message_handling' THEN claimed * MIN(count, 10) * 10
        WHEN bonus_id = 'youth_participation' THEN claimed * MIN(count, 5) * 20
        WHEN bonus_id = 'gota_bonus' THEN claimed * (count * 5 + CASE WHEN count >= 10 THEN 100 ELSE 0 END)
        WHEN bonus_id = 'web_submission' THEN claimed * 50
        WHEN bonus_id = 'safety_officer' THEN claimed * 100
        WHEN bonus_id = 'site_responsibilities' THEN claimed * 50
        ELSE claimed * 100
    END), 0) FROM bonus_claims`).Scan(&bonusPoints)
    ```
    On query error: `bonusPoints = 0` (safe fallback — bonus_claims table might not exist yet).

    2. Change `score := rawPoints * multiplier` (line 45) to `score := (rawPoints * multiplier) + bonusPoints` per ARRL rules section 7.3.

    3. Add `"bonus_points": bonusPoints` to the JSON response map (after `"raw_points": rawPoints` at line 68, before `"multiplier": multiplier` at line 69).

    per D-05: **Modify `internal/cabrillo/cabrillo.go`** — Add bonus points to Cabrillo export:

    1. After multiplier calculation (line 77) and before CLAIMED-SCORE (line 78), add the SAME bonus points query as above in stats.go (the exact same SQL CASE expression). On error: `bonusPoints = 0`.

    2. Change `score := rawPoints * multiplier` (line 78) to `score := (rawPoints * multiplier) + bonusPoints`.

    3. After CLAIMED-SCORE line (line 79) and before the QSO rows query (line 81), add a block that queries `bonus_claims` rows and appends SOAPBOX lines. Pattern:
       - Query: `SELECT bonus_id, claimed, count FROM bonus_claims WHERE claimed = 1 ORDER BY bonus_id`
       - For each row, compute points using the same bonus point logic (call model.CalculateBonusPoints or inline the calculation). Look up bonus name from model.DefaultBonuses (import `"github.com/jeremy/mlogger-fd/internal/model"`). Write `buf.WriteString(fmt.Sprintf("SOAPBOX: Bonus: %s = %d pts\n", item.Name, pts))`.
       - After the loop, write `buf.WriteString(fmt.Sprintf("SOAPBOX: Total Bonus Points = %d\n", bonusPoints))`.
       - If query fails, skip bonus lines silently (bonus_claims table may not exist yet).

    per D-05: **Modify `frontend/src/lib/components/StatsBar.svelte`** — Add bonus stat block:

    1. Insert a new stat block between the "Pts" stat (lines 30-33) and "Mult" stat (lines 34-37):
    ```svelte
    <div class="stat bonus">
        <span class="stat-label">Bonus</span>
        <span class="stat-value">{stats.bonus_points || 0}</span>
    </div>
    ```

    2. Add CSS for bonus color at the end of the style block: `.bonus .stat-value { color: #cc8800; }` and `:global([data-theme='dark']) .bonus .stat-value { color: #ffaa00; }` — gold/amber to distinguish from Pts (primary color) and Score (success color). The `:global()` wrapper is needed since `data-theme` is on `document.body`, outside this component's scope.

    **ARRL formula note:** The CONTEXT.md phrase on line 97 says `(raw_points + bonus_points) * multiplier`. This contradicts ARRL rules section 7.3 ("bonus points will be added to the score, after the multiplier is applied"). Per RESEARCH.md recommendation A1 (line 856), implement the ARRL-correct post-multiplier formula: `score = (rawPoints * multiplier) + bonusPoints`. This is the correct behavior for ARRL submission.
  </action>
  <verify>
    <automated>go test ./internal/handler/ -run TestStats -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'bonusPoints' internal/handler/stats.go` returns at least 1
    - `grep -c 'bonus_claims' internal/handler/stats.go` returns at least 1
    - `grep -c 'bonus_points' internal/handler/stats.go` returns at least 1
    - `grep 'score :=' internal/handler/stats.go` contains `rawPoints * multiplier) + bonusPoints` (post-multiplier addition)
    - `grep -c 'bonusPoints' internal/cabrillo/cabrillo.go` returns at least 1
    - `grep -c 'SOAPBOX: Bonus:' internal/cabrillo/cabrillo.go` returns at least 1
    - `grep -c 'SOAPBOX: Total Bonus Points' internal/cabrillo/cabrillo.go` returns at least 1
    - `grep 'score :=' internal/cabrillo/cabrillo.go` contains `rawPoints * multiplier) + bonusPoints` (post-multiplier addition)
    - `grep -c 'bonus_points' frontend/src/lib/components/StatsBar.svelte` returns at least 1
    - `grep -c 'cc8800\|ffaa00' frontend/src/lib/components/StatsBar.svelte` returns at least 1
    - Bonus stat block appears between Pts and Mult in StatsBar template (verify line ordering)
    - `go test ./internal/handler/ -run TestStats -count=1` exits 0
    - `go test ./internal/cabrillo/ -run TestGenerate -count=1` exits 0
  </acceptance_criteria>
  <done>stats.go returns bonus_points in response and uses ARRL-correct score formula. cabrillo.go includes bonus in CLAIMED-SCORE and SOAPBOX lines. StatsBar.svelte displays Bonus stat block with gold color. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API (GET /api/stats) | Untrusted client reads stats; bonus_points query must handle empty/missing bonus_claims table gracefully |
| Client→API (GET /api/export/cabrillo) | Untrusted client triggers export; bonus query must not fail export if bonus_claims table is missing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | stats.go — bonus_claims query error could corrupt stats response | mitigate | `COALESCE(SUM(...), 0)` ensures 0 bonus when table empty; `if err != nil { bonusPoints = 0 }` safe fallback — per RESEARCH.md pitfall 6 |
| T-04-05 | Tampering | cabrillo.go — bonus_claims query error could break export | mitigate | Same safe fallback: `if err != nil { bonusPoints = 0 }` and skip SOAPBOX loop silently on query error |
| T-04-06 | Information Disclosure | cabrillo.go — SOAPBOX lines expose bonus claim internals | accept | SOAPBOX is a standard Cabrillo comment format; bonus data is not sensitive (honor system contest) |
| T-04-07 | Denial of Service | StatsBar.svelte — large bonus_points value rendering | accept | Values are bounded by ARRL rules (max ~4000); no risk of DOM overload |
</threat_model>

<verification>
After both tasks complete:
1. Start server, open browser
2. Click ★ Bonuses in header → panel expands, shows 18 bonus items with toggle checkboxes
3. Toggle "Media Publicity" ON → click Save → see "Saved!" toast for 2 seconds
4. Verify StatsBar now shows a "Bonus" stat showing 100 (between Pts and Mult)
5. Click Export Cabrillo → open downloaded .cbr file → verify CLAIMED-SCORE includes bonus, SOAPBOX lines list claimed bonuses
6. Reload page → bonus panel still shows Media Publicity as claimed (localStorage + server persistence)
</verification>

<success_criteria>
- BonusTracker expandable panel opens/closes from header, following StationConfig pattern
- All 18 bonus items displayed with appropriate toggle checkbox or checkbox+count input
- Saving bonuses persists to server (PUT /api/bonuses) and localStorage (fdlogger_bonus_claims)
- StatsBar shows bonus_points in gold between Pts and Mult
- Score = (rawPoints * multiplier) + bonusPoints per ARRL rules
- Cabrillo CLAIMED-SCORE reflects bonus, SOAPBOX lines detail each claimed bonus
</success_criteria>

<output>
Create `.planning/phases/04-field-day-features-testing/04-02-SUMMARY.md` when done
</output>
